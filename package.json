{
    "name": "flipped-coding",
    "version": "0.1.0",
    "scripts": {
        "build": "npm install",
        "start": "npm run build && concurrently --kill-others \"node --inspect ./server.js\" \"http-server\""
    },
    "dependencies": {
        "concurrently": "^6.0.2",
        "cors": "^2.8.5",
        "express": "^4.17.3",
        "johnny-five": "^2.0.0",
        "path": "^0.12.7",
        "serialport": "^9.0.7"
    }
}
